import {uniqBy} from 'rambda'

describe('R.uniqBy', () => {
  it('happy', () => {
    const result = uniqBy()

    result // $ExpectType number
  })
  it('curried', () => {
    const result = uniqBy()

    result // $ExpectType number
  })
})
