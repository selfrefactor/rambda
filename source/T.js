/**
 * A function that always returns `true`. Any passed in parameters are ignored.
 *
 * @func
 * @category Function
 * @sig * -> Boolean
 * @param {*}
 * @return {Boolean}
 * @example
 *
 *      R.T(); //=> true
 */
export function T(){
  return true
}
